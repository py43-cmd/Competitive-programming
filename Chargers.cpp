#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, maxcap;
    cin >> t >> maxcap;

    for (int i = 0; i < t; i++)
    {
        int q, t, c;
        cin >> q >> t >> c;
        if (t == 1)
        {
            
        }
        else
        {
        }
    }
}